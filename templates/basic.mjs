// Template: plain TypeScript project (Node CLI, library, scripts).
// The preset with zero options — framework-neutral, all blocks on.
//
// Copy to your repo as eslint.config.mjs (the import below already uses the package name):
//   pnpm add -D eslint-config-agentic eslint typescript

import agentic from 'eslint-config-agentic';

export default agentic();
